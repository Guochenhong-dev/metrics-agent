# 经营指标分析报告

问题：最近7天净实收为什么下降，按区域分析

指标：净实收

口径：支付额－退款额

时间字段：支付按支付日、退款按退款日

当前周期：2026-08-25 至 2026-08-31；前期：2026-08-18 至 2026-08-24

权限区域：华东、华北、华南；数据版本：demo-v1-20260831；数据更新时间：2026-09-15T06:53:45.712541406Z；报告生成：2026-09-15T06:53:46.928474822Z

筛选：区域=null；品类=null；渠道=null；订单状态=null（null表示不额外筛选）

## 已计算发现

- [E1] 净实收：当前 1119060.00元，前期 1470100.00元，变化 -351040.00元；相对变化 -23.88%。
- [E2] 按变化贡献绝对值排序，华东对总体变化的贡献为 -351040.00元。这表示统计变化来源，不证明业务因果。
- [E4] 第二维度为同一范围的独立拆解，不是与第一维度交叉分析；可在计划中加筛选后继续下钻。
- 现金流口径，不是会计确认收入。退款可来自更早支付的订单，净实收可能为负。

## 相关解释与待验证假设

报告已将总体变化按维度分解，以下为相关变化与待验证解释；不能仅凭聚合数据证明因果。

- 待验证：华东的业务量、金额或分母结构变化可能与总体波动相关，需进一步核验。；证据 [E1, E2]；验证方式：按相同口径核对该分类订单和退款来源；固定其他维度后复查，排除数据未齐和观察期不足。

## 主维度变化

|分类|当前|前期|变化贡献|贡献率%|
|---|---:|---:|---:|---:|
|华东|119510.00|470550.00|-351040.00|100.00|
|华北|488150.00|488150.00|0.00|0.00|
|华南|511400.00|511400.00|0.00|0.00|

## 查询依据

### E1 当前周期与前一等长周期汇总

```sql
SELECT 'TOTAL' AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?)
```

参数：[2026-08-25, 2026-08-31, 华东, 华北, 华南]

聚合结果：[Raw[group=TOTAL, numerator=111906000, denominator=0, rows=844]]

```sql
SELECT 'TOTAL' AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?)
```

参数：[2026-08-18, 2026-08-24, 华东, 华北, 华南]

聚合结果：[Raw[group=TOTAL, numerator=147010000, denominator=0, rows=1080]]

### E2 区域变化拆解

```sql
SELECT region AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?) GROUP BY region ORDER BY region
```

参数：[2026-08-25, 2026-08-31, 华东, 华北, 华南]

聚合结果：[Raw[group=华东, numerator=11951000, denominator=0, rows=116], Raw[group=华北, numerator=48815000, denominator=0, rows=359], Raw[group=华南, numerator=51140000, denominator=0, rows=369]]

```sql
SELECT region AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?) GROUP BY region ORDER BY region
```

参数：[2026-08-18, 2026-08-24, 华东, 华北, 华南]

聚合结果：[Raw[group=华东, numerator=47055000, denominator=0, rows=352], Raw[group=华北, numerator=48815000, denominator=0, rows=359], Raw[group=华南, numerator=51140000, denominator=0, rows=369]]

### E3 按日趋势对照

```sql
SELECT biz_date AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?) GROUP BY biz_date ORDER BY biz_date
```

参数：[2026-08-18, 2026-08-31, 华东, 华北, 华南]

聚合结果：[Raw[group=2026-08-18, numerator=20315000, denominator=0, rows=151], Raw[group=2026-08-19, numerator=20090000, denominator=0, rows=147], Raw[group=2026-08-20, numerator=20990000, denominator=0, rows=155], Raw[group=2026-08-21, numerator=21155000, denominator=0, rows=162], Raw[group=2026-08-22, numerator=21800000, denominator=0, rows=157], Raw[group=2026-08-23, numerator=21180000, denominator=0, rows=148], Raw[group=2026-08-24, numerator=21480000, denominator=0, rows=160], Raw[group=2026-08-25, numerator=18259000, denominator=0, rows=137], Raw[group=2026-08-26, numerator=15423000, denominator=0, rows=117], Raw[group=2026-08-27, numerator=15547000, denominator=0, rows=120], Raw[group=2026-08-28, numerator=15251000, denominator=0, rows=123], Raw[group=2026-08-29, numerator=15412000, denominator=0, rows=111], Raw[group=2026-08-30, numerator=16154000, denominator=0, rows=111], Raw[group=2026-08-31, numerator=15860000, denominator=0, rows=125]]

### E4 品类独立拆解

```sql
SELECT category AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?) GROUP BY category ORDER BY category
```

参数：[2026-08-25, 2026-08-31, 华东, 华北, 华南]

聚合结果：[Raw[group=洗衣机, numerator=37127000, denominator=0, rows=283], Raw[group=热水器, numerator=25717000, denominator=0, rows=276], Raw[group=空调, numerator=49062000, denominator=0, rows=285]]

```sql
SELECT category AS group_key, COALESCE(SUM(paid_cents - refund_cents),0) AS num, COALESCE(SUM(0),0) AS den, COUNT(*) AS row_count FROM v_finance_events WHERE biz_date >= ? AND biz_date <= ? AND region IN (?,?,?) GROUP BY category ORDER BY category
```

参数：[2026-08-18, 2026-08-24, 华东, 华北, 华南]

聚合结果：[Raw[group=洗衣机, numerator=49115000, denominator=0, rows=366], Raw[group=热水器, numerator=34210000, denominator=0, rows=354], Raw[group=空调, numerator=63685000, denominator=0, rows=360]]

模式：规则解释；阈值规则：相对变化绝对值达到20%时提示波动；基期为0或无记录时不判定
